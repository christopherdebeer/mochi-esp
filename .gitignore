# ── ESP-IDF build artifacts ──
firmware/build/
firmware/sdkconfig
firmware/sdkconfig.old

# Managed components are regenerated from idf_component.yml on first
# build (idf.py reconfigure). Don't track — see firmware/components/
# README.md for what's vendored vs managed.
firmware/managed_components/

# ── val.town local state ──
.vt/

# ── Editor / OS ──
.DS_Store
.vscode/
.cursorrules
.claude/
*.swp
*~

# ── Node / Deno (val.town side) ──
node_modules/
